namespace :project do

  desc "Create the projects on the server"
  task :create do
    #on roles(:app) do
    #  execute "mkdir -p #{shared_path}/config"
     # upload! StringIO.new(File.read("config/database.yml")), "#{shared_path}/config/database.yml"
    #end
  end


end
